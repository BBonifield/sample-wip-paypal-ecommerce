# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  controllers:
    application:
      already_logged_in: "You are already signed in.  You cannot perform that action while signed in."
      not_authenticated: "You are not signed in.  You must be signed in to perform that action."
  time:
    formats:
      short_date: "%m/%d/%y"
